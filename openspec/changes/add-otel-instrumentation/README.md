# add-otel-instrumentation

OpenTelemetry spans on every Graph API call and every webhook handler invocation, with redacted attributes (hashed phone_number_id) and no-op fallback when no global tracer is registered.
