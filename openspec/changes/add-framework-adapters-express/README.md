# add-framework-adapters-express

Replace the Phase 0 ./express stub with a working createWhatsAppMiddleware: GET handshake, raw-body POST receiver with HMAC verify, 30s ack with async handler dispatch.
