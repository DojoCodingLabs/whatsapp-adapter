# bootstrap-whatsapp-adapter

Establish package skeleton (TS strict, ESM+CJS, vitest, ESLint+Prettier, tsup, CI), shared types/error hierarchy, GRAPH_API_VERSION constant, and stub specs for cloud-api-client + webhook-receiver.
