/**
 * Bin entry — Phase C0 skeleton.
 *
 * The real implementation lands in Phase C1: env-var config
 * loader, `WhatsAppClient` instantiation, `WhatsAppMcpServer`
 * + `StdioServerTransport.connect`. This placeholder exists
 * so the workspace build wires the bin field correctly.
 */

process.stderr.write("@dojocoding/whatsapp-mcp is not yet implemented (Phase C0 skeleton).\n");
process.exit(1);
